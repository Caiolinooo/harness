# demo-app

Target workspace for The Last Harness `coding` vertical.

```bash
pnpm harness -- run -w ./examples/demo-app -v coding --brief "Build a TypeScript todo CLI with add/list/done and vitest tests."
```
